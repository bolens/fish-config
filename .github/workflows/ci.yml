name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  fish-syntax:
    uses: bolens/.github/.github/workflows/reusable-fish.yml@5c0011f4e1045c431c3d2e1a1df0a5bd859daeb5
